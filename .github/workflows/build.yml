# A simple action usage
name: Build Kernel
on:
  workflow_dispatch:

jobs:
  Build-Kernel:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Build Kernel
        uses: tungnt017/kernel_build_action@main
        with:
          kernel-url: https://github.com/darohribi/A54B
          kernel-branch: main
          config: s5e8835-m35xxx_defconfig
          arch: arm64
          aosp-gcc: true
          aosp-clang: true
          android-version: 13
          anykernel3: true
